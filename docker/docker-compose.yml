services:
  influxdb:
    image: influxdb:2
    container_name: wind_influxdb
    ports:
      - "8086:8086"
    volumes:
      - ./docker/influxdb/data:/var/lib/influxdb2
      - ./docker/influxdb/config:/etc/influxdb2
    restart: unless-stopped
